# Optional override. The app defaults to relative /api.
NEXT_PUBLIC_API_BASE_URL=/api

# Backend URL used when the app runs as a native Android/iOS app via Capacitor.
# Relative URLs don't work inside the native WebView, so an absolute URL is required.
NEXT_PUBLIC_NATIVE_API_URL=https://confession-app-a9eu.onrender.com/api
